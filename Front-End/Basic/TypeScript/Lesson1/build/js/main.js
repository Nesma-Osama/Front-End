"use strict";
// set up tsc --init
// change root directory to ./src and outDir directory to ./build/js
// ts file inside src
// write on the terminal tsc -w
//  ,"include": ["src"] in json file to if we create file outside scr it will ignore it
////////////////////////////////////////////////////////////////////////////////////////  
let Name = 'nesosa';
console.log(Name);
