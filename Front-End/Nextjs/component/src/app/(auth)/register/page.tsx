export default function Register()
{
    return<h1>Register</h1>
}
//login and register therir url is /login not /auth/login because I used Route Group
// mean put them in the same folder but not change their url by using () for the auth folder